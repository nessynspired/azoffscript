Gate IP-sensitive content from crew — planner-only access

Crew members can no longer see brand IP (captions, hashtags, hooks,
prompts, market gaps, brand moats, do-not-chase rules, etc.).
Only planners/admins can access these.

NAVIGATION (PortalNav.tsx):
  Removed from CREW_MORE menu:
    - Prompts
    - Discovery
    - Growth System
  Brand Locker stays visible to crew but captions/hashtags tabs
  are hidden (crew only sees logos, mascot, colors, formats,
  templates).

GROWTH SYSTEM (all 9 documents):
  New layout.tsx at /portal/growth/ wraps all growth pages in
  <PlannerOnly>. Crew who navigate directly to any growth URL
  see a "planner-only" locked message instead.

DISCOVERY LIBRARY (/portal/discovery):
  Wrapped in <PlannerOnly label="The Discovery Library">.
  Crew see locked message.

PROMPTS LIBRARY (/portal/prompts):
  Wrapped in <PlannerOnly label="The Prompt Library">.
  Crew see locked message.

BRAND LOCKER (/portal/brand-locker):
  Captions and Hashtags tabs hidden from crew. Crew see:
  Logos, Mascot, Colors, Formats, Gear Templates.
  Planners see all tabs including Captions and Hashtags.

CLIP DETAIL MODAL (Run Sheet):
  Caption, idea_text (brief), and do_not_post_notes are now
  planner-only. Crew see the clip title, video, template
  instructions, deadlines, and status — but not the caption,
  brief, or do-not-post notes. This prevents crew from copying
  captions/hashtags/strategy from scheduled clips.

NEW COMPONENT:
  src/components/PlannerOnly.tsx — reusable wrapper that checks
  member.role === "admin" || member.can_plan_content. Shows a
  locked message to non-planners, renders children for planners.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
