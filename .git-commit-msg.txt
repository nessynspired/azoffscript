Fix What's Moving + multiple file uploads + connect to planned clip

WHAT'S MOVING FIX:
- The filter was too restrictive — only showed clips with file_path or link,
  excluding ideas. Now shows all dropped content clips (matching Recent Drops).
- Added sort by created_at so newest drops show first.

MULTIPLE FILE UPLOADS:
- Drop page now supports picking multiple videos/images at once.
- File picker accepts video/*, audio/*, image/* with multiple selection.
- Shows list of selected files with remove buttons and "Add more files".
- All files upload to Supabase storage, first file stored in file_path,
  all files stored in production_notes.files array.
- Clip detail modal shows all uploaded files with video players.

CONNECT TO PLANNED CLIP:
- Made more prominent with border and clearer label:
  "Which content piece is this for?"

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
