Fix VideoPlayer to show images + show actual error messages

- VideoPlayer now detects image files (.jpg, .jpeg, .png, .gif, .webp,
  .heic, .heif) and renders them as <img> instead of <video>. Latasha
  dropped JPEG screenshots, not videos — the video player was trying to
  play them and failing silently.
- VideoPlayer now shows the actual error message from Supabase storage
  instead of just "Could not load video", so we can diagnose issues.
- The files DO exist in storage (confirmed via service role client).
  Ronnie's 4MB mp4 and Latasha's 4 JPEGs are all there.

Note: Latasha dropped 4 files but only 1 was saved to file_path because
the production_notes column doesn't exist yet. Run the combined migration
(supabase/run-all-migrations.sql) to fix this for future drops.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
