Hide IP gates silently — no locked messages

Planners were seeing "admin-only" locked messages on the
Calendar Builder right panel, Growth System, Discovery, and
Prompts pages. That rubs people the wrong way. Now non-admins
just see nothing — the content is silently hidden.

AdminOnly component now renders null for non-admins (no fallback
message). The `label` prop is still accepted for backward
compatibility but ignored.

Calendar Builder: the right panel (libraries) is now completely
absent for non-admin planners — no locked card, no message.
Planners can still:
  - Schedule templates from the Ready Bank onto the calendar
  - Delete clips from the calendar
  - Add content (free-form)
  - Set deadlines, assign crew, change clip status

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
