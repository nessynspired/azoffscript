Change IP gates from planner-only to admin-only

Planners are still crew members who could copy your brand IP
(hooks, captions, market gaps, brand moats, prompts). Changed
all IP-sensitive gates from PlannerOnly to AdminOnly so only
the brand owner (admin) can see them.

NEW: AdminOnly component (src/components/AdminOnly.tsx)
  Same as PlannerOnly but checks role === "admin" only.
  Planners and crew both see the locked message.

CHANGED FROM PLANNER-ONLY TO ADMIN-ONLY:
  - Growth System (all 9 documents) — layout.tsx now uses AdminOnly
  - Discovery Library — wrapped in AdminOnly
  - Prompts Library — wrapped in AdminOnly
  - Brand Locker captions/hashtags tabs — admin-only
  - ClipDetailModal caption, brief, do-not-post notes — admin-only
  - "Edit with Libraries" button in ClipDetailModal — admin-only
  - "Create from Library" button on Run Sheet — admin-only
  - Calendar Builder right panel (libraries) — admin-only
    (non-admin planners see a locked message instead)
  - Calendar Builder "Create from Library" button — admin-only

NAVIGATION:
  Added adminOnly flag to NavItem type. Marked these as adminOnly
  in PLANNER_MORE: Editing Recipes, Prompts, Discovery, Growth
  System. Non-admin planners don't see these in their nav.
  Admins see everything.

WHAT PLANNERS CAN STILL DO:
  - Schedule clips from the Ready Bank
  - Add content (free-form, no library items)
  - Set deadlines, assign crew, change clip status
  - See the calendar, studio flow, assignment board
  - Manage the Run Sheet

WHAT PLANNERS CAN NO LONGER SEE:
  - Hooks, prompts, caption frameworks (growth libraries)
  - Market gaps, brand moats, do-not-chase rules
  - Discovery library (captions, search keywords, comment prompts)
  - Prompts library
  - Brand Locker captions and hashtags
  - Clip captions, briefs, and do-not-post notes
  - "Edit with Libraries" / "Create from Library" features

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
