Fix production_notes error + combined migration SQL

The "could not find the production_notes column" error happened because
the production_notes column was never added to the database. The multi-file
upload code tried to insert into it, causing the drop to fail.

Fix: moved the production_notes update to AFTER the clip is created, and
made it non-blocking — if the column doesn't exist, the clip is still
saved and a warning is logged to console.

Also created supabase/run-all-migrations.sql — a single file that applies
ALL pending database migrations at once:
  1. production_notes column (multiple file uploads)
  2. drop_purpose column (raw footage vs content)
  3. Fixed RLS policy on agreement_signatures (signature duplicate fix)
  4. Recreated clips_with_meta view with new columns

Run this ONE file in the Supabase SQL Editor to fix everything.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
